# Use: SPRING_PROFILES_ACTIVE=local ./mvnw spring-boot:run
# Backend starts without the Python MCP server (port 8000) and without /api/chat.
# Calculator, funds, auth sync, saved calculations, etc. still work.

spring.ai.mcp.client.enabled=false
app.chat.enabled=false
